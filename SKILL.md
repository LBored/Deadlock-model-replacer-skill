---
name: game-model-replacer
description: "制作或修复游戏角色、服装和道具的模型替换 Mod：提取目标资源，适配骨架与材质，处理实时物理，编译并打包验证。适用于已有模型替换已有游戏对象；含经过实际制作检验的 Deadlock / Source 2 专项流程，其他引擎先验证专用工具链。"
---

# 游戏模型替换 Mod

将用户提供的模型制作成指定游戏对象的可安装替换包，同时保存可编辑工程、构建脚本及验证记录。按用户当前阶段进入工作：新制作、缺陷修复、增量 UI/语音或最终合包。跟随用户语言交流。

## 起点与适用边界

先从会话和本地工程确定游戏/构建版本、目标对象、替换素材、选定基线及允许修改范围。只问仍缺失且影响执行的信息。已有可用包应从用户指定版本和哈希继续，不以最新修改时间或通用工作文件名代替基线。

本技能通用的是制作与验证方法；实际提取、编译和加载取决于游戏。Source 2 有专项经验；其他引擎先建立小规模导出—编译—加载证据，不能承诺任意游戏都可直接替换。只识别出引擎仍不足以认定工具兼容；记录具体游戏版本、格式和加载器。缺工具时给出官方来源和需要的能力。

路径全部由当前用户环境决定。沿用其存储规则，启动工具前配置临时目录、缓存、配置和下载目录。不要依赖技能作者或案例贡献者的工程、模型、插件路径或游戏安装。

复制 [assets/project-config.example.json](assets/project-config.example.json) 到工作区并填入实测信息；未验证项目保留 null / unknown。可用 `source/ reference/ work/ export/ stage/ dist/ logs/ validation/ releases/` 组织项目，但不强制搬迁已有工程。

## 制作路线

1. **锁定范围和基线。** 区分外观替换与技能/玩法改变，记录哪些模型、道具、动画、材质、UI 和声音必须保留。读取 [references/discovery-and-baselines.md](references/discovery-and-baselines.md)，建立目标路径和依赖清单。
2. **验证引擎链路。** Source 2 读取 [references/source2.md](references/source2.md)。新引擎只先验证最小网格；需要实时物理时再验证一个简化代理。已通过的同版本链路可复用，不要求用户反复确认。未过游戏门槛时继续材质、源文件等独立工作。
3. **适配与绑定。** 读取 [references/mesh-and-materials.md](references/mesh-and-materials.md)。优先适配网格至目标骨架，保留目标骨名、父子关系与绑定矩阵。分别校正身体、手指、服装、头发。单独保留嵌入原人物网格的武器/技能道具。
4. **物理与动作。** 用户需要物理，或正在修复穿模/散架时读取 [references/physics-and-debugging.md](references/physics-and-debugging.md)。衣物惯性、碰撞和死亡布娃娃分开验证。Blender 缓存不能替代游戏运行物理。
5. **导出与编译。** 按目标工具限制处理权重、坐标轴、单位、材质、LOD；保留完整动画依赖与挂点。导出后从成品独立读取数据，不以 Blender 中的正确状态代替编译结果。
6. **封包与验收。** 读取 [references/release-validation.md](references/release-validation.md)。用隔离的 stage 封包、检查路径范围与基线哈希，并交付具体安装/回滚方法。只有用户要求时再做 [references/ui-and-voice.md](references/ui-and-voice.md) 中的可选扩展。

案例只提供已发生问题的证据，不定义通用默认值。仅在用户处理 Deadlock、要求参考本项目，或症状与既有案例直接相关时，读取 [references/cases/deadlock-mutsumi-graves.md](references/cases/deadlock-mutsumi-graves.md)。不要从案例继承路径、工具版本、骨骼数、资源数量、参数或用户授权。

## 持续迭代

将每次反馈对应到包哈希、动作、时刻/截图和具体区域。先区分拓扑缺口、蒙皮折叠、背面剔除、透明遮罩与物理穿插，再动对应层。改变裙型/骨架/动作幅度等可能影响已接受外观的修改，保留前版并明确取舍。用户要求 A/B 时，先构建并归档 A，再从 A 派生 B。

每个检查结果标记 `pass / fail / not_run`，记录证据和工具版本。分开报告“编译通过”“离线通过”“游戏验证通过”。游戏复测尚未进行时交付候选包和聚焦复测清单，不将封包成功表述为最终通过。确切失败阶段、可复现命令和下一步留在项目状态文件。

## 附带工具

- `scripts/asset_audit.py`：Python 标准库工具，对目录或 Valve VPK v1/v2 生成 SHA-256 清单；VPK 会逐文件检查 CRC。比较两份清单时区分新增、改变、删除、未变；可约束允许变化路径。默认不在报告中写绝对输入路径。**不验证包级 MD5、不解析游戏依赖、不负责解包或封包。** 用法见发布参考。
- `scripts/blender_audit.py`：在 Blender 中读取网格、骨架、蒙皮和材质信息，输出 JSON，不修正或保存输入模型。用于定位空材质、未绑定顶点、超限骨影响及不同骨架的绑定差异；默认不在报告中写绝对输入或贴图路径。不模拟引擎动画或布料。用法见网格参考。

技能包只携带流程与自编辅助脚本，不携带游戏资源、第三方模型、声音权重或私人图片。制作本地替换包不等同于用户要求公开发布这些素材。
